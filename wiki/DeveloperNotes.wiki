#summary Developer notes and implementation details

= State management =

Default dice rolling method is stored as a DataDocument attached to the wavelet.

When user clicks on configuration gadget, it sets a property to itself. This fires a DOCUMENT_CHANGED event. Robot captures it, reads that gadget property and sets a new DataDocument to wavelet. 

When a roll is made, robot retrieves that DataDocument from wave and uses it for rolling.

= Framework =

To be done...

= Extensibility =

To be done...